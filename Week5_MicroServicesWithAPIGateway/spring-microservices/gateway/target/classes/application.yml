server:
  port: 8083

spring:
  application:
    name: api-gateway

  cloud:
    gateway:
      routes:
        - id: account-service
          uri: http://localhost:8080
          predicates:
            - Path=/accounts/**

        - id: loan-service
          uri: http://localhost:8081
          predicates:
            - Path=/loans/**
